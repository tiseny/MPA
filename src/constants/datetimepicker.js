export const FORMAT = 'yyyy-mm-dd'
export const MINVIEW = 2
export const LANGUAGE = 'zh-CN'
export const AUTOCLOSE = true

export const DEFAULT_DATETIMEPICKER_OPTION = {
	format: FORMAT,
	minview: MINVIEW,
	language: LANGUAGE,
	autoclose: AUTOCLOSE
}